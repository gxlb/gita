package main_test

/*
import (
	"gita/exec"
	"gita/gitlib"
	"os"
	"regexp"
	"testing"
)

var exprGetSrcBranch = regexp.MustCompile(`^.*?gita.*?\s+sync\s+.*?(?:-s|-src)(?:\s*=\s*|\s+)(?P<SRC>[-\w\./]+)\s+.*$`)

func getSrcBranch(cmd string) string {
	elems := exprGetSrcBranch.FindAllStringSubmatch(cmd, 1)
	if len(elems) > 0 {
		return elems[0][1]
	}
	return ""
}
*/
